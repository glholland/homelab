apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
metadata:
  labels:
    machineconfiguration.openshift.io/role: worker
  name: 99-qemu-guest-agent
spec:
  config:
    ignition:
      version: 3.2.0
    systemd:
      units:
        - name: qemu-guest-agent.service
          enabled: true
