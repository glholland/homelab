apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-cert
  namespace: grafana
spec:
  secretName: grafana-tls
  dnsNames:
    - grafana.garrettholland.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
