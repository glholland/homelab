# cloudnative-pg

The [CloudNativePG](https://cloudnative-pg.io/) operator, installed from the
upstream release manifest (pinned to 1.30.0) rather than via OLM -- no
compatible OLM catalog is enabled on this cluster.

Provides the `postgresql.cnpg.io/v1 Cluster` CRD used by `kubernetes/zitadel`.
