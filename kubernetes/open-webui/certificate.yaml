apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: open-webui-cert
  namespace: open-webui
spec:
  dnsNames:
    - chat.garrettholland.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: open-webui-tls
