# AVP placeholders only -- never commit real values.
# Requires GCP Secret Manager entries in project home-26300 (812524172865):
#   - pterodactyl-app-key            (Laravel APP_KEY, format "base64:...")
#   - pterodactyl-db-password        (shared with pterodactyl-mariadb-secret#MARIADB_PASSWORD)
#   - pterodactyl-mail-password      (currently unused live -- MAIL_MAILER=log, no SMTP configured)
apiVersion: v1
kind: Secret
metadata:
  name: pterodactyl-panel-secret
  namespace: pterodactyl
type: Opaque
stringData:
  APP_KEY: <path:projects/812524172865/secrets/pterodactyl-app-key#pterodactyl-app-key>
  DB_PASSWORD: <path:projects/812524172865/secrets/pterodactyl-db-password#pterodactyl-db-password>
  # Mail is disabled (MAIL_MAILER=log) and this value is empty in-cluster,
  # so there is no Secret Manager entry for it.
  MAIL_PASSWORD: ""
